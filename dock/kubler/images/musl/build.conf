# valid builder repo id, comment out for namespace default
#BUILDER="${NAMESPACE}/bob"
# valid image repo id (i.e. kubler/busybox or "scratch")
IMAGE_PARENT="${NAMESPACE}/busybox"
BUILDER_CAPS_SYS_PTRACE="true"
