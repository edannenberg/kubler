#!/bin/bash

# ARG_VERSION([echo $0 v0.9.0])
# ARG_OPTIONAL_SINGLE([working-dir],[w],[Where to look for namespaces or images, default: current directory])
# ARG_OPTIONAL_BOOLEAN([debug])
# ARGBASH_SET_INDENT([    ])
